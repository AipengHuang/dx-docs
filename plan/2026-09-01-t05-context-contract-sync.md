# T05 上下文检索契约同步

## 目标

保持 dx-docs 为 Platform 专用的内网知识执行服务，仅同步 T05 所需的共享契约并确认现有混合检索接口满足调用要求。

## 实施步骤

- [x] 核对现有混合检索请求与 chunk/source 响应。
- [x] 仅同步共享 OpenAPI 契约，不新增公开接口。
- [x] 保持执行句柄、组织范围与服务令牌校验不变。
- [x] 运行 Go 测试和契约漂移检查。

## 影响范围

- 共享 OpenAPI 生成产物（如有变化）
- 内网检索契约验证

## 验证方式

- `go test ./...`
- Platform 契约同步/漂移检查。

## 进度

- 当前：确认现有混合检索已满足 T05，共享契约已同步，全量 Go 测试通过。

## 最终结果

- dx-docs 无业务源代码改动，继续仅提供 `/internal/v1` 检索接口。
- `go test ./...` 与 Platform 契约漂移检查均通过。
