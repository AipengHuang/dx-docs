# T06 引用契约同步

## 目标

保持 dx-docs 为 Platform 专用知识执行服务，仅同步 T06 的共享引用契约；不新增浏览器接口或第二套鉴权。

## 实施步骤

- [x] 确认现有检索响应已提供文件、chunk 和 offset 元数据。
- [x] 同步 Platform OpenAPI 与生成契约。
- [x] 保持执行句柄、组织范围和服务令牌边界不变。
- [x] 运行 Go 测试和契约漂移检查。

## 影响范围

- 共享 OpenAPI 生成产物
- 生成的 Go 契约类型

## 验证方式

- `go test ./...`
- Platform 契约同步检查。

## 进度

- 当前：完成。

## 最终结果

- dx-docs 继续只返回 Platform 已授权检索结果，不新增浏览器引用接口或第二套权限体系。
- 共享契约已同步；`go test ./...` 和 Platform 契约漂移检查通过。
